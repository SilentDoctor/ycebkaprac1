<Page x:Class="ProductionCaptchaSystem.Views.AdminView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Панель администратора">

    <Grid Background="#F5F5F5" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопками -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Панель администратора" 
                      FontSize="24" 
                      FontWeight="Bold"
                      Foreground="#2196F3"
                      VerticalAlignment="Center"
                      Margin="0,0,30,0"/>

            <Button Content="📥 Импорт данных" 
                   Width="150" 
                   Height="40"
                   Background="#4CAF50"
                   Foreground="White"
                   BorderThickness="0"
                   FontSize="14"
                   Cursor="Hand"
                   Click="ImportButton_OnClick"
                   Margin="5"/>

            <Button Content="👥 Пользователи" 
                   Width="150" 
                   Height="40"
                   Background="#2196F3"
                   Foreground="White"
                   BorderThickness="0"
                   FontSize="14"
                   Cursor="Hand"
                   Click="UsersButton_OnClick"
                   Margin="5"/>

            <Button Content="📊 Просмотр таблиц" 
                   Width="170" 
                   Height="40"
                   Background="#FF9800"
                   Foreground="White"
                   BorderThickness="0"
                   FontSize="14"
                   Cursor="Hand"
                   Click="ViewTablesButton_OnClick"
                   Margin="5"/>
        </StackPanel>

        <!-- Контент (DataGrid или Лог импорта) -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#DDDDDD" 
                BorderThickness="1"
                CornerRadius="5"
                Padding="15">

            <Grid>
                <!-- DataGrid для пользователей -->
                <DataGrid x:Name="UsersDataGrid" 
                         Visibility="Collapsed"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Логин" Binding="{Binding Username}" Width="150"/>
                        <DataGridCheckBoxColumn Header="Админ" Binding="{Binding IsAdmin}" Width="80"/>
                        <DataGridCheckBoxColumn Header="Заблокирован" Binding="{Binding IsBlocked}" Width="120"/>
                        <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedAt}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Лог импорта -->
                <ScrollViewer x:Name="ImportLogScroll" 
                             Visibility="Visible"
                             VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ImportLogTextBlock"
                              FontFamily="Consolas"
                              FontSize="12"
                              Foreground="#333"
                              Text="Нажмите 'Импорт данных' для загрузки файлов из папки Data/"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Page>
