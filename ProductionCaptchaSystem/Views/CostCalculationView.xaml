<Page x:Class="ProductionCaptchaSystem.Views.CostCalculationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Расчёт стоимости продукции">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Расчёт стоимости продукции" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Выберите продукт:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="ProductComboBox" Width="300" SelectionChanged="ProductComboBox_SelectionChanged"
                      DisplayMemberPath="Name"/>
            <Button Content="Рассчитать" Margin="15,0,0,0" Padding="15,5" Click="CalculateButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="MaterialsDataGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Материал" Binding="{Binding MaterialName}" Width="*"/>
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding Unit}" Width="80"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Цена, руб." Binding="{Binding Price, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Стоимость, руб." Binding="{Binding Cost, StringFormat=N2}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Итого себестоимость:" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock x:Name="TotalCostText" Text="0.00 руб." FontSize="16" FontWeight="Bold" 
                       Foreground="Green" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
